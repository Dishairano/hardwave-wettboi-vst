name: Build & Release

on:
  push:
    tags:
      - 'v*'
  workflow_dispatch:
    inputs:
      version:
        description: 'Version tag (e.g., v0.1.0)'
        required: true

env:
  CARGO_TERM_COLOR: always

jobs:
  build-linux:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Install Rust
        uses: dtolnay/rust-toolchain@stable
        with:
          targets: x86_64-unknown-linux-gnu

      - name: Install Linux dependencies
        run: sudo apt-get update && sudo apt-get install -y libgtk-3-dev libwebkit2gtk-4.1-dev

      - name: Build
        run: cargo xtask bundle hardwave-wettboi --release

      - name: Package
        run: |
          cd target/bundled
          tar -czvf hardwave-wettboi-linux-x64.tar.gz hardwave-wettboi.vst3 hardwave-wettboi.clap

      - name: Upload
        uses: actions/upload-artifact@v4
        with:
          name: hardwave-wettboi-linux-x64
          path: target/bundled/hardwave-wettboi-linux-x64.tar.gz

  build-windows:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Install Rust + Windows MSVC target
        uses: dtolnay/rust-toolchain@stable
        with:
          targets: x86_64-pc-windows-msvc

      - name: Install cargo-xwin
        run: cargo install cargo-xwin --locked

      - name: Build (MSVC cross-compile, with GUI)
        run: cargo xwin build --release --target x86_64-pc-windows-msvc -p hardwave-wettboi

      - name: Bundle VST3 and CLAP
        run: |
          DLL=target/x86_64-pc-windows-msvc/release/hardwave_wettboi.dll
          # VST3 bundle
          mkdir -p target/bundled/hardwave-wettboi.vst3/Contents/x86_64-win
          cp "$DLL" target/bundled/hardwave-wettboi.vst3/Contents/x86_64-win/hardwave-wettboi.vst3
          # CLAP bundle
          cp "$DLL" target/bundled/hardwave-wettboi.clap

      - name: Package
        run: |
          cd target/bundled
          zip -r hardwave-wettboi-windows-x64.zip hardwave-wettboi.vst3 hardwave-wettboi.clap

      - name: Upload
        uses: actions/upload-artifact@v4
        with:
          name: hardwave-wettboi-windows-x64
          path: target/bundled/hardwave-wettboi-windows-x64.zip

  build-macos-x64:
    runs-on: macos-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Install Rust
        uses: dtolnay/rust-toolchain@stable
        with:
          targets: x86_64-apple-darwin

      - name: Build
        run: cargo xtask bundle hardwave-wettboi --release --target x86_64-apple-darwin

      - name: Package
        run: |
          cd target/bundled
          tar -czvf hardwave-wettboi-macos-x64.tar.gz hardwave-wettboi.vst3 hardwave-wettboi.clap

      - name: Upload
        uses: actions/upload-artifact@v4
        with:
          name: hardwave-wettboi-macos-x64
          path: target/bundled/hardwave-wettboi-macos-x64.tar.gz

  build-macos-arm64:
    runs-on: macos-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Install Rust
        uses: dtolnay/rust-toolchain@stable
        with:
          targets: aarch64-apple-darwin

      - name: Build
        run: cargo xtask bundle hardwave-wettboi --release --target aarch64-apple-darwin

      - name: Package
        run: |
          cd target/bundled
          tar -czvf hardwave-wettboi-macos-arm64.tar.gz hardwave-wettboi.vst3 hardwave-wettboi.clap

      - name: Upload
        uses: actions/upload-artifact@v4
        with:
          name: hardwave-wettboi-macos-arm64
          path: target/bundled/hardwave-wettboi-macos-arm64.tar.gz

  release:
    needs: [build-linux, build-windows, build-macos-x64, build-macos-arm64]
    runs-on: ubuntu-latest
    permissions:
      contents: write

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Download all artifacts
        uses: actions/download-artifact@v4
        with:
          path: artifacts

      - name: List artifacts
        run: find artifacts -type f

      - name: Get version
        id: version
        run: |
          if [ "${{ github.event_name }}" = "workflow_dispatch" ]; then
            echo "version=${{ github.event.inputs.version }}" >> $GITHUB_OUTPUT
          else
            echo "version=${GITHUB_REF#refs/tags/}" >> $GITHUB_OUTPUT
          fi

      - name: Create Release
        uses: softprops/action-gh-release@v1
        with:
          tag_name: ${{ steps.version.outputs.version }}
          name: WettBoi ${{ steps.version.outputs.version }}
          draft: false
          prerelease: false
          generate_release_notes: true
          files: |
            artifacts/hardwave-wettboi-linux-x64/*.tar.gz
            artifacts/hardwave-wettboi-windows-x64/*.zip
            artifacts/hardwave-wettboi-macos-x64/*.tar.gz
            artifacts/hardwave-wettboi-macos-arm64/*.tar.gz
          body: |
            ## WettBoi ${{ steps.version.outputs.version }}

            Wet signal processor VST3/CLAP plugin — powered by Hardwave Studios.

            ### Downloads

            | Platform | File |
            |----------|------|
            | Windows x64 | `hardwave-wettboi-windows-x64.zip` |
            | Linux x64 | `hardwave-wettboi-linux-x64.tar.gz` |
            | macOS x64 (Intel) | `hardwave-wettboi-macos-x64.tar.gz` |
            | macOS arm64 (Apple Silicon) | `hardwave-wettboi-macos-arm64.tar.gz` |

            ### Installation

            **Windows:**
            1. Extract the zip
            2. Copy `hardwave-wettboi.vst3` folder to `C:\Program Files\Common Files\VST3\`
            3. Copy `hardwave-wettboi.clap` to `C:\Program Files\Common Files\CLAP\`

            **macOS:**
            1. Extract: `tar -xzf hardwave-wettboi-macos-*.tar.gz`
            2. Copy `hardwave-wettboi.vst3` to `~/Library/Audio/Plug-Ins/VST3/`
            3. Copy `hardwave-wettboi.clap` to `~/Library/Audio/Plug-Ins/CLAP/`

            **Linux:**
            1. Extract: `tar -xzf hardwave-wettboi-linux-x64.tar.gz`
            2. Copy to `~/.vst3/` and `~/.clap/`

      - name: Sync version to WettBoi server
        run: |
          curl -sf -X POST \
            -H "Authorization: Bearer ${{ secrets.DEPLOY_SECRET }}" \
            -H "Content-Type: application/json" \
            -d "{\"slug\":\"wettboi\",\"version\":\"${{ steps.version.outputs.version }}\"}" \
            https://wettboi.hardwavestudios.com/api/updates/version-sync
